# Synthetic evaluator corpus

All content in this directory is **SYNTHETIC** and must never be described as a real person, organization, contract, or investigation.

Files:

- `project.json` — synthetic project metadata
- `source.json` — synthetic source metadata
- `claim.json` — synthetic candidate claim with exact-anchor fields
